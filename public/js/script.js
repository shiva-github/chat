(function($) {
	$(document).ready(function() {
		// var socket = io('http://localhost:3001');
		// var UserName = Math.floor(Math.random() * 10);

		// $('#send_message').click(function() {
			
		// });
		
		// function onFetchMessages(socketio, messages) {
		// 	let val = $('#message_input').val()
		// 	let usermsg = {
		// 		"user": 'test' + UserName,
		// 		"message": val
		// 	}
		// 	if (val.length != 0) {
		// 		socket.emit('chat message', usermsg);
		// 		$('#message_input').val('');
		// 	}
		// }
	})
})(jQuery);